This is a basic Maven build tool based project example.

Place all source files under `src/main/java` directory.

Edit `pom.xml` file if you need extra library dependencies.

To build the project run:

    mvn package
    
To run test:

    mvn test
